// Package schema provides dependency-free builders and typed conveniences for
// Schema.org JSON-LD documents.
//
// Typed constructors cover common web schemas. Object provides an escape hatch
// for every other Schema.org type and property without requiring this package
// to mirror the complete and evolving Schema.org vocabulary.
package schema
